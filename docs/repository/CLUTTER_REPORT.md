# Repository Clutter Report

> **Status:** Approved · **Version:** 1.0 · **Owner:** Qoid Rif'at
> **Last Updated:** 2026-08-04 · **Related:** [REPOSITORY_AUDIT](REPOSITORY_AUDIT.md), [LEGACY_REPORT](LEGACY_REPORT.md), [GIT_COMMIT_GUIDE](GIT_COMMIT_GUIDE.md)
> **Audience:** Maintainers

---

## 1. Summary

Clutter is classified into two buckets: **(A) tracked clutter** (visible in git — needs action before publishing) and **(B) disk-only clutter** (already git-ignored — safe, cleanup optional).

No file was deleted during this audit (read-only mandate). Every removal below requires maintainer confirmation.

---

## 2. Tracked Clutter (visible to anyone who clones)

### 2.1 🔴 `public/logout-debug-viewport.png` (75 KB) — debug artifact

- Tracked: `git ls-files public/logout-debug-viewport.png`
- Origin: logout-flow debugging (June 2026); sibling artifacts (`logout-success-recording.webm`, `logout-success-recording-report.json`, `recordings/`) are already git-ignored — this PNG is the only leftover that slipped through.
- **Status: RESOLVED 2026-08-04** — untracked (`git rm --cached`) + `.gitignore` rule added. File kept on disk; safe to delete locally.

### 2.2 🟡 `.vscode/settings.json` — empty config

- Tracked: 1 file, content is literally `{}` (zero value).
- **Recommendation:** harmless; either keep (signals VS Code is supported) or gitignore `.vscode/`. Low priority.

### 2.3 🟡 `scripts/verify-*.mjs` — one-off verification scripts superseded by E2E

| Script | Superseded by |
|---|---|
| `verify-admin-monitoring.mjs` | `e2e/admin-metrics-auth.spec.ts` |
| `verify-agent-search-auth.mjs` | `e2e/agent-search-auth.spec.ts` |
| `verify-cache-panel.mjs` | `e2e/admin-cache.spec.ts` |
| `verify-ai-search-browser.mjs` | `e2e/core-pages.spec.ts` (AI Search coverage) |

- **Recommendation:** verified **0 references** to `verify-*` in `.github/workflows/e2e.yml` and `package.json` (grep evidence, 2026-08-04) — safe to move to `docs/archive/` as historical snippets or delete. Keep `seedE2eDataset.mjs`, `applyTursoSchema.mjs`, `backupTurso.mjs`, `restoreTurso.mjs`, `runBackup.cmd`, `seedGmailSyncRunsFromLogs.mjs`, `e2e-stability-gate.sh` — these are operational.

### 2.4 🟡 Duplicate/misplaced assets

- `docs/assets/screenshots/` (21 PNG) and `e2e/visual/visual-regression.spec.ts-snapshots/` (10 PNG) serve **different purposes** (marketing gallery vs CI baselines) — keep both, but note ~2.6 MB of PNGs dominate tracked size.
- No duplicate images detected between the two sets (different filenames/viewports).

### 2.5 🟢 `agent.md` + `task-list.md` at repo root

- `agent.md` (20 KB) — AI-agent instructions. Industry convention now commits these as `AGENTS.md`; if kept, rename for discoverability.
- `task-list.md` — personal dev to-do. Not product documentation.
- **Recommendation:** rename `agent.md` → `AGENTS.md` (optional) and move `task-list.md` to `docs/archive/` or gitignore.

---

## 3. Disk-Only Clutter (git-ignored — never in history)

| Item | Size | Ignored by | Action |
|---|---|---|---|
| `.dev-server*.log` ×6 | ~1.1 MB | `.dev-server*.log` | Delete locally (contains stack traces / env echo) |
| `backups/` | — | `backups/` | Keep (contains Turso dumps); never commit |
| `dist/` (incl. `dist/recordings/*.webm`, `dist/logout-success-recording.webm`) | — | `dist/` | Delete locally; regenerated by `npm run build` |
| `cashflow.db` + `tmp-e2e-test.db-journal` | 274 KB | `*.db`, `*.db-journal` | Delete locally (dev data); Turso is the real DB |
| `.env.local` (real secrets!) | — | `*.local` | Keep for dev; never commit |
| `server/.env` + `server/*service-account*.json` | — | `server/*.env`, `server/*.json` | Keep for dev; never commit |
| `node_modules/` + `server/node_modules/` | ~108 MB | `node_modules` | Delete to free space |
| `playwright-report/` | — | `playwright-report/` | Delete locally |
| `logout-success-recording*` (root) | — | already removed from disk | — |

> **Note:** `git status --short` shows **0 untracked files** — proof that every non-tracked item above is correctly ignored.

---

## 4. Non-Issues (checked, clean)

- No `*.bak` / `*.old` / `*.tar` / `*.zip` / scratch files in the repo.
- No `coverage/`, `.cache/`, `.vite/` directories.
- No duplicate `package.json` outside root + `server/` (both legitimate).
- No TODO/FIXME dump files at root.

---

## 5. Recommended Cleanup Order

1. R1 critical first: scrub archived API key (see [SECURITY_REPOSITORY_REVIEW](SECURITY_REPOSITORY_REVIEW.md)).
2. `git rm --cached public/logout-debug-viewport.png` + gitignore (clutter R2.1).
3. Decide `.kiro/`, `.agents/`, `task-list.md` (see [LEGACY_REPORT](LEGACY_REPORT.md)).
4. Move superseded `verify-*.mjs` to archive or delete.
5. Local disk cleanup (log files, dist, node_modules) — purely for developer convenience.

---

## References

- [REPOSITORY_AUDIT.md](REPOSITORY_AUDIT.md)
- [LEGACY_REPORT.md](LEGACY_REPORT.md)
- [GIT_COMMIT_GUIDE.md](GIT_COMMIT_GUIDE.md)
