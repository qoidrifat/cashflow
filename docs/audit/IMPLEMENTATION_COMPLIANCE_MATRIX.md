# Implementation Compliance Matrix — CashFlow E2E Modernization Audit

> Audit READ-ONLY · Tanggal: 1 Agustus 2026 · Status: **Complete / Partial / Missing / Unknown**
> Metode: verifikasi langsung terhadap source code + query DB read-only. Tidak ada file yang dimodifikasi (deliverable audit ditulis ke `docs/audit/`).
> **Diperbarui 2 Agustus 2026**: ditambahkan Bagian 10 (Server Auth Fixes — Post-Audit) — status fix `resolveAgentSearchUser` + regression guard E2E, dan Bagian 11 (Admin Metrics Fixes — Post-Audit) — status fix `resolveAdmin` + `metricsService` raw SQL Turso. **Final decommission 2026-08-02**: project Supabase `bwczweuomlwmgwgrsadt` (cashflow) **dihapus** via CLI (`supabase projects delete --yes`, verified `projects list` tidak lagi menampilkannya) + cleanup frontend tuntas (0 referensi supabase di `src/`).

| # | Feature | Expected | Actual | Status | Evidence | Notes |
|---|---|---|---|---|---|---|
| 1a | `playwright.config.ts` | Exists, correct config | ✅ Exists | **Complete** | `playwright.config.ts` (root) | `testDir ./e2e`, `workers: 1`, `retries: 1`, `expect.timeout 20s`, reporter list+html, `forbidOnly` CI, screenshot/trace/video retain-on-failure, actionTimeout 15s, navigationTimeout 30s |
| 1b | `webServer` + ports | Auto-start Vite + API | ✅ 2 webServer entries | **Complete** | `playwright.config.ts` L37–49 | `npm run dev:server` → `http://localhost:5181/api/health`; `npm run dev` → `http://localhost:5180`; `reuseExistingServer: true` keduanya; timeout 60s |
| 1c | `tsconfig.e2e.json` | Typecheck terpisah e2e | ✅ Exists | **Complete** | `tsconfig.e2e.json` | `extends ./tsconfig.json`, `types: ["node"]`, include `playwright.config.ts` + `e2e/**/*.ts` |
| 1d | package.json scripts | 5 script e2e | ✅ 5/5 ada | **Complete** | `package.json` scripts | `test:e2e`, `test:e2e:gmail`, `test:e2e:transactions`, `test:e2e:dashboard`, `test:e2e:typecheck` — semua ada dan valid |
| 1e | Browsers | Chromium terinstal | ✅ | **Complete** | `node_modules/playwright`, run suite sukses | Suite berjalan headless Chromium (viewport 1440×900) |
| 2a | `e2e/helpers/mintSession.ts` | Mint sesi Better Auth ke Turso | ✅ Exists | **Complete** | `e2e/helpers/mintSession.ts` | Tulis baris `session` ke Turso (`userAgent='e2e-test'`), cookie `token.base64url(HMAC-SHA256(secret, token))` sesuai skema `server/lib/auth.js`; `cleanupTestSessions()` hapus sesi e2e |
| 2b | `e2e/helpers/authContext.ts` | Cookie login helper | ✅ Exists | **Complete** | `e2e/helpers/authContext.ts` | `suppressOnboarding` (localStorage `cashflow-onboarding-done` via addInitScript) + `injectSessionCookie` (httpOnly, sameSite Lax, domain localhost) + `setupAuthContext` |
| 2c | `e2e/helpers/pagination.ts` | Helper pagination bersama | ✅ Exists | **Complete** | `e2e/helpers/pagination.ts` | `counterRegexFor/getListCountText/listTotalFrom/listRangeFrom/waitListTotal/waitListRange` berbasis keyword `'transaksi'`/`'email'`; `expect.poll` anti-flaky |
| 2d | Helper tambahan | Dedup pageerror | ✅ Bonus | **Complete** | `e2e/helpers/errors.ts` | `collectPageErrors(page)` → `{ all(), expectClean() }` dipakai seluruh 17 test (6 spec) |
| 3a | gmail-sync summary | Cocok API ground truth | ✅ | **Complete** | `e2e/gmail-sync.spec.ts` test 1 | `/api/gmail/logs?includeSummary=1` → cards `Diterima/Perlu Review/Dilewati/Ditolak` = `api.summary.*`; total 519 |
| 3b | gmail-sync filter | List count = summary | ✅ | **Complete** | `e2e/gmail-sync.spec.ts` test 2 | `clickFilterAndWaitResponse` (wait response API + URL searchParams presisi) → Perlu Review `needs_review`, Diterima Otomatis `auto_accepted`, Semua `null` |
| 3c | gmail-sync pagination | Counter X-Y dari N benar | ✅ | **Complete** | `e2e/gmail-sync.spec.ts` test 3 | Halaman 1–6 (pageSize 100), `waitListRange`, indikator "Halaman X dari Y", tombol Berikutnya disabled di akhir |
| 4a | transaksi pagination | Counter benar | ✅ | **Complete** | `e2e/transactions.spec.ts` test 3 | Halaman 2–6 (pageSize 50, total 284), `waitListRange` + `pageApi.data.length` |
| 4b | transaksi filter | List count per tipe | ✅ | **Complete** | `e2e/transactions.spec.ts` test 2 | Pemasukan 86 / Pengeluaran 131 / Semua 284 via API |
| 4c | transaksi API consistency | Ground truth API | ✅ | **Complete** | `e2e/transactions.spec.ts` test 1 | `/api/transactions/paginated`; cookie eksplisit pada fixture `request` |
| 5a | dashboard balance | Total Saldo cocok API | ✅ | **Complete** | `e2e/dashboard.spec.ts` test 1 | Replikasi `calculateBalance()` dari `/api/transactions?limit=50`; banding strip non-digit (anti-locale-flaky) |
| 5b | dashboard stat cards | Tampil | ✅ | **Complete** | `e2e/dashboard.spec.ts` test 1 | Total Saldo, Pemasukan/Pengeluaran Bulan Ini, Sisa Budget |
| 5c | dashboard quick actions | Tampil | ✅ | **Complete** | `e2e/dashboard.spec.ts` test 2 | Pemasukan, Pengeluaran, Scan Gmail, Laporan |
| 5d | dashboard latest tx | Tampil | ✅ | **Complete** | `e2e/dashboard.spec.ts` test 2 | Transaksi Terbaru + Lihat Semua + min 1 item |
| 6a | Cookie auth helper | ✅ | **Complete** | `e2e/helpers/authContext.ts` | |
| 6b | Request cookie forwarding | ✅ | **Complete** | Semua spec `request.get(..., { headers: { Cookie: ... } })` | Fixture `request` context terpisah dari browser → cookie eksplisit |
| 6c | networkidle removal | ✅ | **Complete** | Spec: `waitForLoadState('domcontentloaded')` + komentar "bukan networkidle — bisa hang karena HMR" | |
| 6d | pageerror listener | ✅ | **Complete** | `e2e/helpers/errors.ts` + `pageErrors.expectClean()` di 17 test (6 spec) | |
| 6e | expect.poll | ✅ | **Complete** | `pagination.ts` `waitListTotal/waitListRange` | + `clickFilterAndWaitResponse` untuk filter gmail |
| 6f | Pagination helper extraction | ✅ | **Complete** | `pagination.ts` dipakai transactions + gmail | Satu sumber kebenaran |
| 7a | `npm run build` | Lulus | ✅ | **Complete** | Bukti sesi: exit 0 (17.75s; variasi 8.51–17.75s) | Tidak dieksekusi ulang di audit ini (mode read-only) |
| 7b | `npm run lint` | Lulus | ✅ | **Complete** | `npm run lint` = `tsc --noEmit` → exit 0 | |
| 7c | `npm run typecheck` | Script ada & lulus | ✅ **Complete** | Script `typecheck` alias ditambahkan di `package.json` (`tsc --noEmit`); `npm run typecheck` exit 0 (terverifikasi 2026-08-02) | Gap kecil tertutup — bagian 1–8 kini 33/33 |
| 7d | `npm run test:e2e` | Lulus | ✅ | **Complete** | Bukti sesi audit: 8/8 passed 3× berurutan (43.1/41.7/42.6s) — 0 flaky; run pasca-P1: **17/17 passed 3× (1.0m/57.5s/59.9s) — 0 flaky** setelah `agent-search-auth.spec.ts` + `admin-metrics-auth.spec.ts` + `core-pages.spec.ts` ditambahkan | |
| 7e | `tsc --noEmit` | Lulus | ✅ | **Complete** | exit 0 (src + e2e) | |
| 8a | Transactions dataset | 284 | ✅ 284 (query read-only: `transactions: 284`) | **Complete** | Pinned `expect(api.total).toBe(284)` + fetch API | **Hybrid**: total di-fetch dari API lalu di-assert ke nilai pinned — regression guard + API-driven |
| 8b | Gmail Logs dataset | 519 | ✅ 519 (query: `gmail_logs: 519`) | **Complete** | Pinned `expect(api.total).toBe(519)` | Hybrid, sama seperti 8a |
| 8c | Sync Runs dataset | 2 | ✅ 2 (query: `gmail_runs: 2`) | **Complete** | Tidak di-assert di spec manapun | Angka 2 terverifikasi di DB; belum ada assertion spec untuk runs |
| 9a | CI Pipeline (GitHub Actions) | Workflow E2E | ✅ **Complete** | `.github/workflows/e2e.yml` | quality (lint+typecheck+build) → e2e (server `npm ci`, playwright install, Turso secrets, concurrency global, artifacts report + test-results) |
| 9b | E2E coverage Budgets/Reports/Notifications/Realtime | Spec ada | ✅ **Complete** | `e2e/core-pages.spec.ts` ditambahkan 2026-08-02 — smoke test 3 halaman (Budgets, Reports, Notifications) via cookie-login: render tanpa pageerror + assert elemen kunci; sekaligus validasi runtime halaman yang diedit saat cleanup frontend | 3/3 passed; Realtime (SSE) tetap ter-exercise via halaman Notifications + NotificationBell (bukan assertion dedicated) |
| 9c | Stability validation 3× | 0 flaky | ✅ **Complete** | Bukti sesi + `docs/e2e/STABILITY_REPORT.md` | Race condition di `GmailSyncPage.tsx` difix (`paginatedRequestIdRef`) |
| 9d | Dynamic assertions (hapus pinned) | API-driven penuh | ✅ **Complete** | `e2e/helpers/fixtures.ts` — satu-satunya tempat pinned values (284/519/86/131) sebagai regression guard terdokumentasi; spec memakai `PINNED` + `PINNED_DESCRIPTION` (expect message) | Validasi utama tetap API-driven; pin terpusat 1 file (bukan tersebar di spec) |
| 10a | `resolveAgentSearchUser` fix (server) | Supabase JWT → `req.user` Better Auth | ✅ **Complete** | `server/index.js` L763–778 | Sebelum: validasi Bearer via `getSupabaseServerClient()` (referensi `createClient` tak ter-import → `ReferenceError` → **500 di semua route user-scoped** `/api/agent-search/*`). Sesudah: `req.user.id` dari `authMiddleware` (pola sama `resolveAdmin`); `required` → throw 401 `AGENT_SEARCH_INVALID_REQUEST`; opsional → `null` (tab help/docs anonim). Helper mati `getSupabaseServerClient()`/`getBearerToken()` dihapus |
| 10b | Dead Supabase code removal | Hapus import `createClient` dari agentSearchService | ✅ **Complete** | `server/services/agentSearchService.js` L7 | `createClient` di-import tapi **0× dipakai** (grep count = 1 = baris import). `node --check` lulus; runtime server **0 reader** `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` |
| 10f | Decommission Supabase penuh (2026-08-02) | Hapus key + arsip + tanda legacy + **hapus project** | ✅ **Complete** | `server/.env`, `server/.env.example`, `docs/supabase-migration/` (dihapus), skrip migrasi (banner LEGACY), `server/package.json`+lock (`@supabase/supabase-js` di-uninstall), **project dihapus** (`supabase projects delete bwczweuomlwmgwgrsadt --yes` → "Deleted project: cashflow") | Supabase project retired penuh: (1) key dihapus total dari env (bukan sekadar migration-only); (2) folder arsip migrasi dihapus; (3) skrip migrasi ditandai LEGACY; (4) dead dependency `@supabase/supabase-js` dihapus dari server (root tetap punya untuk skrip legacy); (5) `GMAIL_BACKGROUND_SYNC_SETUP.md` + `GENAI_APP_BUILDER_CASHFLOW_SETUP.md` ditandai/bersih; (6) **project `bwczweuomlwmgwgrsadt` dihapus via CLI 2026-08-02** — verified `supabase projects list` tersisa hanya 2 project lain (`gysxuttsxyxofimsmqty` qoidrifat's Project, `fpiceveqbvgaxdtxxbaq` payrollpro) yang **tidak disentuh**; (7) **cleanup frontend tuntas** (audit terpisah, 2026-08-02) — rincian: **Env & CI**: `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` dihapus dari `.env.example` (via sed, CRLF) + `.github/workflows/e2e.yml`; **Stub dihapus**: `src/config/supabase.ts` + `src/lib/supabase/client.ts` + folder kosong `src/lib/supabase/` (rmdir, 0 consumer terverifikasi); **`initSupabase()`** import+call dihapus dari **5 pages** (Budgets, Dashboard, Reports, Transactions, GmailSync — grep = 0 masing-masing); **Constants**: `SUPABASE_TABLES` + `ERROR_MESSAGES.SUPABASE_CONFIG` dihapus dari `src/config/constants.ts` (0 pengguna); **Rename**: `supabaseMappers.ts`→`mappers.ts` (7 import diperbarui), `getCurrentSupabaseUser()`→`getCurrentAuthUser()` (gmailService); **Teks pengguna**: LandingPage ("RLS di server"→"akses data per-user"), PrivacyPage (2×), AutoSyncStatus (hapus referensi Edge Function Supabase); **Komentar stale**: 12 komentar GmailSyncPage + useAuthStore, useSessionExpiryStore, App, sessionErrors, receiptScanService, sse. **Verifikasi akhir**: `grep -rni 'supabase' src/` = **0 match**, `tsc --noEmit` exit 0, `npm run build` ✓ (17.28s), E2E 11/11 (baseline pra-spec admin metrics), health API 5181 + FE 5180 = 200/200; audit ulang 2026-08-02: BERSIH |
| 10c | E2E auth gate regression guard | Spec `/api/agent-search/*` 401 vs lolos | ✅ **Complete** | `e2e/agent-search-auth.spec.ts` | 3 test, API-level via fixture `request` + cookie eksplisit: (1) tanpa cookie → tab user-scoped `query`/`answer` + `sync-transactions/gmail-logs/receipts` = **401** (bukan 500); (2) tanpa cookie → tab `help` anonim + `sync-docs` publik = **bukan 401/500**; (3) dengan cookie minted → 4 tab + sync = **bukan 401/500**. Menangkap kelas regresi ReferenceError otomatis |
| 10d | package.json script agent-search | `test:e2e:agent-search` | ✅ **Complete** | `package.json` scripts | `playwright test e2e/agent-search-auth.spec.ts` — **3 passed (8.4s)**, `tsc -p tsconfig.e2e.json` OK, code review 2 putaran (nit relative-path/type-import/simetri tab diterapkan) |
| 10e | Verifikasi browser AI Search | Page render + query + sync tanpa 500/401 | ✅ **Complete** | `scripts/verify-ai-search-browser.mjs` (TEMP) | Login cookie E2E; `/api/agent-search/answer` 400 (valid), `sync-transactions` **200**, health **200**; **0 response 5xx/401**, 0 pageerror. Server 5181 + Vite 5180 hidup |
| 11a | `resolveAdmin` fix (server) | Supabase JWT → `req.user` Better Auth + `ADMIN_EMAILS` | ✅ **Complete** | `server/index.js` L1515–1535 | Sebelum: validasi Supabase JWT terpisah (tidak kompatibel setelah decommission). Sesudah: `req.user` dari `authMiddleware`; `user?.email` kosong → **401**; email tidak ada di `getAdminEmails()` (env `ADMIN_EMAILS`, di-import dari `./config/metricsConfig.js`) → **403**; return `{ userId, email }`. Dipakai oleh **6 route admin metrics** (L1555, 1569, 1584, 1612, 1634, 1657) |
| 11b | `metricsService` rewrite (raw SQL Turso) | Supabase query-builder → raw SQL prepared statements | ✅ **Complete** | `server/services/metricsService.js` | Komentar header eksplisit: "sebelumnya memakai Supabase query-builder API yang tidak kompatibel dengan libSQL — semua endpoint admin metrics 500. Kini raw SQL dengan prepared statements." `getMetricsClient()` → `getTurso()` (`../lib/turso.js`); **0 referensi** `createClient`/`@supabase` (grep count = 0) |
| 11c | Frontend path alignment admin | `feature-calls` → `/feature/:feature/calls` | ✅ **Complete** | `src/services/adminMetrics.ts` L51 | `getFeatureCalls` → `/api/admin/metrics/feature/${feature}/calls`; `summary`/`ai-usage`/`feature-health`/`alerts` cocok dengan 6 route server (L1552–1655) |
| 11d | Verifikasi browser Admin Monitoring | Page render + summary cards + riwayat per fitur tanpa 401 | ✅ **Complete** | `scripts/verify-admin-monitoring.mjs` (TEMP) | Login cookie E2E (user pertama = `qoidrifat23@gmail.com` = `ADMIN_EMAILS`, otomatis lolos gate admin); verifikasi `/admin/monitoring` memuat summary cards + riwayat panggilan per fitur TANPA 401 |
| 11e | E2E auth gate regression guard admin metrics | Spec `/api/admin/metrics/*` 401 vs lolos | ✅ **Complete** | `e2e/admin-metrics-auth.spec.ts` | 3 test, API-level via fixture `request` + cookie eksplisit: (1) tanpa cookie → semua 6 endpoint = **401**; (2) cookie non-admin (`mintSessionCookieForEmail`) → **403** + `ok:false` + `code: ADMIN_METRICS_403`; (3) cookie admin → **200** + `ok:true`. Hardening `expect.poll` anti-flaky (10s). **Diverifikasi stabil**: 3× full suite 14/14 — 0 flaky (setelah fix `authMiddleware` P0) |
| 11f | package.json script `test:e2e:admin` | Script terpisah | ✅ **Complete** | `package.json` scripts | `test:e2e:admin` = `playwright test e2e/admin-metrics-auth.spec.ts` — ikut hijau di 3× full suite 14/14 (0 flaky) setelah fix `authMiddleware` |

---

## Ringkasan Status per Kategori

| Kategori | Complete | Partial | Missing | Unknown |
|---|---|---|---|---|
| 1. Playwright Infrastructure | 5 | 0 | 0 | 0 |
| 2. Shared Helpers | 4 | 0 | 0 | 0 |
| 3. Gmail Sync E2E | 3 | 0 | 0 | 0 |
| 4. Transactions E2E | 3 | 0 | 0 | 0 |
| 5. Dashboard E2E | 4 | 0 | 0 | 0 |
| 6. Infrastructure Improvements | 6 | 0 | 0 | 0 |
| 7. Validation Commands | 5 | 0 | 0 | 0 |
| 8. Dataset Verification | 3 | 0 | 0 | 0 |
| 9. Future Recommendations | 4 | 0 | 0 | 0 |
| 10. Server Auth Fixes (Post-Audit) | 6 | 0 | 0 | 0 |
| 11. Admin Metrics Fixes (Post-Audit) | 6 | 0 | 0 | 0 |
| **Total** | **49** | **0** | **0** | **0** |

**Implementasi inti (bagian 1–8): 33/33 Complete (100%)**.
**Rekomendasi lanjutan (bagian 9): 4/4 selesai** — CI ✅, Stability ✅, dynamic assertions (fixtures.ts), smoke core pages (budgets/reports/notifications).
**Fix pasca-audit (bagian 10): 6/6 Complete** — `resolveAgentSearchUser` (Supabase JWT → Better Auth), dead Supabase code removal, E2E auth gate regression guard, script npm, verifikasi browser, decommission Supabase penuh.
**Fix pasca-audit (bagian 11): 6/6 Complete** — `resolveAdmin` (Supabase JWT → `req.user` + `ADMIN_EMAILS`), `metricsService` raw SQL Turso, path alignment frontend, verifikasi browser, E2E auth gate guard admin metrics (11e), script npm `test:e2e:admin` (11f). 11e/11f selesai setelah **fix `authMiddleware` (P0)** — akar flaky (getSession throw ditelan → 401 transient) diperbaiki: retry sekali + 500 jujur; **3× full suite 14/14 — 0 flaky**.
